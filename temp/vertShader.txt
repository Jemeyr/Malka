#version 130

attribute vec3 position;
uniform vec3 model;
uniform vec3 cameraPosition;
uniform mat4 viewPerspective;

void main(){
	gl_Position = viewPerspective * vec4((position + model), 1.0);
}
		