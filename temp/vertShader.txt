#version 130

attribute vec3 position;
uniform vec3 cameraPosition;
uniform mat4 viewPerspective;

void main(){
	gl_Position = vec4( 0.3 * vec2(position.x + 3.0 * position.z, position.y) - (cameraPosition.xy), 0.0, 1.0 );
}
		